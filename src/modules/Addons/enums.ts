
/**
 * Asset categories
 * ==================================================
 */
export enum AssetCategory {
  NFT = 'nft',
  TOKEN = 'token',
}

/**
 * Arcs standards
 * ==================================================
 */
export enum Arc {
  ARC3 = 'ARC3',
  ARC19 = 'ARC19',
  ARC30 = 'ARC30',
  ARC69 = 'ARC69',
}

/**
 * Note encoding
 * ==================================================
 */
export enum NoteEncoding {
  NONE = 'none',
  TEXT = 'text',
  JSON = 'json',
  MSGPACK = 'msgpack',
}